{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Function - Run & Attach (Debug)",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:azureFunctions.pickProcess}"
    }
  ]
}
